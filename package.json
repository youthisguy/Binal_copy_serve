{
  "name": "binal-copy-trade-service",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "copy-trading service for Binal Bot signals",
  "scripts": {
    "start": "node --env-file=.env local-server.mjs",
    "dev": "node --env-file=.env --watch local-server.mjs"
  },
  "dependencies": {
    "ethers": "^6.13.1",
    "better-sqlite3": "^11.3.0"
  },
  "engines": {
    "node": ">=20.6.0"
  }
}
